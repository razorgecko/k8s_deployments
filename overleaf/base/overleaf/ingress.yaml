apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: overleaf
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging
    traefik.ingress.kubernetes.io/router.middlewares: overleaf-redirect-https@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts: ["placeholder.invalid"]
      secretName: overleaf-tls
  rules:
    - host: placeholder.invalid
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: oauth2-proxy
                port:
                  number: 4180